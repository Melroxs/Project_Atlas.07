# Atlas — Final Pre-Deployment Cleanup Report

**Date:** 2026-08-02
**Branch:** `main` (local)
**Commit:** `chore: production deployment cleanup` (not pushed)
**Scope:** Production-hardening cleanup only — no features, no architecture changes, no business-logic changes.

---

## 1. Environment Variable Naming Fixed

**Problem:** `.env.example` declared `GEMINI_API_KEY` while all code reads `GOOGLE_API_KEY`
(`apps/api/src/lib/env.ts`, `packages/ai/src/generate/provider.ts`).

**Fix:** `.env.example` now standardizes on **`GOOGLE_API_KEY`** with a comment noting Gemini is accessed through it. The obsolete `GEMINI_API_KEY` placeholder is gone. Runtime behavior unchanged.

## 2. Obsolete Environment Variables Removed

`.env.example` was audited against every actual `process.env.*` read in `apps/api/src`, `apps/web/src`, and `packages/*/src` (enumerated via grep) plus the `env.ts` zod schema and the voice provider (`packages/domain/decision/voice/providers/elemental.ts`).

**Removed (never read by code):**
- `GEMINI_API_KEY` — code reads `GOOGLE_API_KEY`
- `JWT_SECRET` — zero references in code
- Duplicate trailing `GROQ_API_KEY=` / `ELEMENTAL_API_KEY=` lines (merge artifact)

**Kept (code-referenced):** `DATABASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `GOOGLE_API_KEY`, `GROQ_API_KEY`, `AI_PROVIDER`, `ELEMENTAL_API_KEY`, `ELEMENTAL_BASE_URL`, `ELEMENTAL_MODEL`, `OPENAI_API_KEY` (legacy, read by `env.ts`), `NEXT_PUBLIC_APP_URL`, `NEXT_PUBLIC_ATLAS_APP_URL` (read by landing page), `NEXT_PUBLIC_API_URL` (read by diagnostics service), `CORS_ORIGIN`, `PORT`, `GIT_COMMIT` (read by diagnostics service).

## 3. Stale Compiled Sources Removed

**Deleted (16 tracked `.js` files with live `.ts` counterparts):**
- `apps/api/src/lib/ai.js`, `apps/api/src/lib/supabase.js`
- `apps/api/src/middleware/auth.js`
- `apps/api/src/routes/{claims,companies,contacts,crud,index,interviews,notes,properties,supplements,tasks,tenant_members,tenants,users}.js`

**Verified:** every deletion has a `.ts` counterpart; all imports are extensionless (TypeScript resolves `.ts` first); grep for `*.js` references in `apps/`, `packages/`, `scripts/` returns **zero matches**; `tsc` build/typecheck confirm no broken imports.

## 4. Regenerated / Normalized Files

- `package-lock.json` — regenerated by `npm install` (resolved ts-jest); committed.
- `apps/landing/src/routeTree.gen.ts` — CRLF-only churn (zero semantic diff); normalized/restored.

## 5. Final Verification (fresh runs)

| Gate | Result |
|---|---|
| `npm install` | ✅ clean, up to date (exit 0) |
| `turbo run build` | ✅ 5/5 packages, exit 0 |
| `turbo run typecheck` | ✅ clean, exit 0 |
| Jest (API) | ✅ 83 passed / 6 skipped, exit 0 |
| Claim intelligence | ✅ 49 passed, 0 failed |
| Operations intelligence | ✅ 89 passed, 0 failed |
| Workflow engine | ✅ 22 passed, 0 failed |
| Broken imports | ✅ none (grep scan + tsc) |
| Duplicate env vars | ✅ none |
| Stale compiled JS | ✅ none (0 untracked `.js` in `apps/api/src`) |

## 6. Repository Status

- **Working tree:** clean after this commit (only the cleanup changes staged/committed).
- **Branch:** `main`; **not pushed** (per instruction).
- **Deployment status:** ✅ **READY FOR VERCEL** — build, typecheck, tests, env audit, and dead-code removal all green. Remaining steps are manual Vercel/Supabase configuration only (Node 20 LTS, env values, redirect URLs, custom domain).
