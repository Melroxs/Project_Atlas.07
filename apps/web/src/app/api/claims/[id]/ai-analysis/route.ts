import { NextRequest, NextResponse} from "next/server";import { ClaimAIAnalysisRequestSchema} from "@/domain/claims/claim.validation";export async function POST(request:NextRequest,context:{ params:{ id:string }}){try{const body = await request.json();const requestData = ClaimAIAnalysisRequestSchema.parse({ ...body, claimId: context.params.id });/*Future connection:Claim AI Engine↓Evidence Engine↓Compliance Engine↓Recommendation Engine*/return NextResponse.json({success:true,analysisRequested:true,data:requestData});}catch(error:any){return NextResponse.json({success:false,error:error.message},{status:400});}}